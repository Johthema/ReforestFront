/** @type {import('next').NextConfig} */
const nextConfig = {
    
        // images: {
        //   domains: ['flagcdn.com', 'upload.wikimedia.org'], // Adicione o domínio da fonte externa aqui
        // },
      

}

module.exports = nextConfig
